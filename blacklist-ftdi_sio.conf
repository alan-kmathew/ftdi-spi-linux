# Prevent the in-tree ftdi_sio serial driver from claiming the FT2232H/FT4232H
# bridge, which otherwise races spi_ft232h at boot and exposes ttyUSB* instead
# of the SPI/GPIO controller. A plain "blacklist" only stops alias autoload, so
blacklist ftdi_sio
install ftdi_sio /bin/true
